overwrite: true

schema: 'https://api-sa-east-1.graphcms.com/v2/cl30k3ynw0ok301xm6n5k5xqr/master'

documents: './src/graphql/**/*.graphql'

generates:
  src/generated/graphql.tsx:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-urql'
    config:
      withHooks: true

hooks:
  afterAllFileWrite:
    - eslint --fix